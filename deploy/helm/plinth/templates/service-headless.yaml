apiVersion: v1
kind: Service
metadata:
  name: {{ include "plinth.fullname" . }}-headless
  labels:
    {{- include "plinth.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    {{- include "plinth.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
