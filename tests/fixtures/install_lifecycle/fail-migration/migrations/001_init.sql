-- Intentional PG syntax error — triggers MIGRATING-stage failure (I.10,
-- I.11). Phrased without `CREATE TABLE` so the static qualified-DDL
-- guard (ICD-0.4.3) doesn't pre-empt PG's parse-error path.
THIS IS NOT VALID SQL AT ALL;
