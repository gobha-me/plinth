---
InheritParentConfig: false

# Curated, low-overlap checks. In particular, do not enable the alias-heavy
# cert-*, cppcoreguidelines-*, hicpp-* and llvm-* profiles wholesale: many of
# them report the same underlying finding under different policy names. Compiler
# diagnostics, sanitizers and clang-format already have their own CI gates.
# bugprone-chained-comparison is intentionally absent: Catch2 implements normal
# binary assertions through a decomposer expression that this check reports as
# a chained comparison, forcing a waiver on essentially every useful assertion.
Checks: >
  -*,
  clang-analyzer-core.*,
  clang-analyzer-cplusplus.*,
  clang-analyzer-deadcode.*,
  clang-analyzer-unix.*,
  -clang-analyzer-unix.BlockInCriticalSection,
  bugprone-argument-comment,
  bugprone-assert-side-effect,
  bugprone-assignment-in-if-condition,
  bugprone-bool-pointer-implicit-conversion,
  bugprone-casting-through-void,
  bugprone-dangling-handle,
  bugprone-implicit-widening-of-multiplication-result,
  bugprone-infinite-loop,
  bugprone-misplaced-operator-in-strlen-in-alloc,
  bugprone-misplaced-pointer-arithmetic-in-alloc,
  bugprone-misplaced-widening-cast,
  bugprone-optional-value-conversion,
  bugprone-posix-return,
  bugprone-return-const-ref-from-parameter,
  bugprone-shared-ptr-array-mismatch,
  bugprone-sizeof-container,
  bugprone-sizeof-expression,
  bugprone-string-constructor,
  bugprone-string-integer-assignment,
  bugprone-string-literal-with-embedded-nul,
  bugprone-suspicious-string-compare,
  bugprone-swapped-arguments,
  bugprone-undefined-memory-manipulation,
  bugprone-unique-ptr-array-mismatch,
  bugprone-use-after-move,
  bugprone-virtual-near-miss,
  performance-avoid-endl,
  performance-faster-string-find,
  performance-for-range-copy,
  performance-inefficient-algorithm,
  performance-inefficient-string-concatenation,
  performance-inefficient-vector-operation,
  performance-move-const-arg,
  performance-move-constructor-init,
  performance-no-automatic-move,
  performance-unnecessary-copy-initialization,
  portability-std-allocator-const,
  # spdlog's base_sink template intentionally declares virtual members whose
  # concrete test sinks instantiate them; Clang 20 reports the dependency
  # interface rather than a first-party portability defect.
  -portability-template-virtual-member-function,
  modernize-avoid-bind,
  modernize-deprecated-ios-base-aliases,
  modernize-make-unique,
  modernize-replace-auto-ptr,
  modernize-replace-disallow-copy-and-assign-macro,
  modernize-use-nullptr,
  modernize-use-override
WarningsAsErrors: '*'
HeaderFilterRegex: '^.*/(include|src)/'
ExcludeHeaderFilterRegex: '^.*/(build[^/]*/|_deps)/'
SystemHeaders: false
FormatStyle: file
